[
  {
    "start": 20,
    "delete": 0,
    "expect": [],
    "insert": [
      "import {",
      "  hidratarComplementosDB, persistirEdicaoOSDB, prepararFinalizacaoOSDB,",
      "  uploadDocumentosCompraDB, uploadLogoEmpresaDB,",
      "} from \"../lib/runtimeApi\";"
    ]
  },
  {
    "start": 627,
    "delete": 1,
    "expect": ["    recarregarSeguro(empresaId).then(({ data, error }) => {"],
    "insert": ["    recarregarSeguro(empresaId).then(async ({ data, error }) => {"]
  },
  {
    "start": 630,
    "delete": 0,
    "expect": [],
    "insert": [
      "      try { data = await hidratarComplementosDB(data, empresaId); } catch (e) { aviso(mensagemErro(e)); }",
      "      if (!ativo) return;"
    ]
  },
  {
    "start": 639,
    "delete": 1,
    "expect": ["    const { data, error } = await recarregarSeguro(empresaId);"],
    "insert": ["    let { data, error } = await recarregarSeguro(empresaId);"]
  },
  {
    "start": 641,
    "delete": 0,
    "expect": [],
    "insert": ["    try { data = await hidratarComplementosDB(data, empresaId); } catch (e) { aviso(mensagemErro(e)); }"]
  },
  {
    "start": 865,
    "delete": 0,
    "expect": [],
    "insert": [
      "        const alvo = ordens.find((o) => o.id === osId);",
      "        if (!alvo) throw new Error(\"Ordem de serviço não encontrada\");",
      "        await prepararFinalizacaoOSDB(alvo, extras, empresaId, usuarioAtual?.id, papel);"
    ]
  },
  {
    "start": 959,
    "delete": 1,
    "expect": ["      try { const salva=await salvarCompraDB(c,empresaId,usuarioAtual?.id); await recarregarDados(); aviso(c.id ? \"Compra atualizada\" : `${salva.numero} registrada. Conta a pagar criada no financeiro.`); setCompraAberta(salva.id); }"],
    "insert": ["      try { const salva=await salvarCompraDB(c,empresaId,usuarioAtual?.id); await uploadDocumentosCompraDB(salva.id,c.anexos||[],empresaId,usuarioAtual?.id); await recarregarDados(); aviso(c.id ? \"Compra atualizada\" : `${salva.numero} registrada. Conta a pagar criada no financeiro.`); setCompraAberta(salva.id); }"]
  },
  {
    "start": 981,
    "delete": 1,
    "expect": ["    garantiaAberta, setGarantiaAberta, abrirGarantia,"],
    "insert": ["    garantiaAberta, setGarantiaAberta, abrirGarantia, real,"]
  },
  {
    "start": 2800,
    "delete": 1,
    "expect": ["    permitido, equipe, usuarioAtual } = p;"],
    "insert": ["    permitido, equipe, usuarioAtual, real, empresaId, aviso, papel } = p;"]
  },
  {
    "start": 2813,
    "delete": 1,
    "expect": ["  const up = (patch) => setOrdens((l) => l.map((x) => (x.id === os.id ? { ...x, ...patch } : x)));"],
    "insert": [
      "  const persistTimer = useRef(null);",
      "  const up = (patch) => {",
      "    const next = { ...os, ...patch };",
      "    setOrdens((l) => l.map((x) => (x.id === os.id ? { ...x, ...patch } : x)));",
      "    if (real) {",
      "      clearTimeout(persistTimer.current);",
      "      persistTimer.current = setTimeout(() => { persistirEdicaoOSDB(next, patch, empresaId, usuarioAtual?.id, papel).catch((e) => aviso(mensagemErro(e))); }, 500);",
      "    }",
      "  };"
    ]
  },
  {
    "start": 3212,
    "delete": 1,
    "expect": ["    setFotos((f) => [...f, ...arquivos.map((a) => ({ id: uid(), nome: a.name, categoria, url: URL.createObjectURL(a) }))]);"],
    "insert": ["    setFotos((f) => [...f, ...arquivos.map((a) => ({ id: uid(), nome: a.name, categoria, url: URL.createObjectURL(a), arquivo: a }))]);"]
  },
  {
    "start": 3480,
    "delete": 1,
    "expect": ["      <p className=\"text-[12px] text-slate-400 mt-2 leading-relaxed\">As fotos ficam apenas nesta sessão até conectarmos o armazenamento de arquivos.</p>"],
    "insert": ["      <p className=\"text-[12px] text-slate-400 mt-2 leading-relaxed\">As fotos novas são enviadas com segurança para a empresa ao salvar ou concluir o atendimento.</p>"]
  },
  {
    "start": 3554,
    "delete": 1,
    "expect": ["                  <p className=\"text-[12px] text-slate-400 mt-3 leading-relaxed\">Os arquivos ficam apenas nesta sessão até conectarmos o armazenamento.</p>"],
    "insert": ["                  <p className=\"text-[12px] text-slate-400 mt-3 leading-relaxed\">Documentos armazenados com acesso restrito à empresa.</p>"]
  },
  {
    "start": 3678,
    "delete": 1,
    "expect": ["        <Field label=\"Documentos\" hint=\"Boleto, nota ou pedido. Ficam apenas nesta sessão até conectarmos o armazenamento.\">"],
    "insert": ["        <Field label=\"Documentos\" hint=\"Boleto, nota ou pedido. Os novos arquivos são enviados ao salvar a compra.\">"]
  },
  {
    "start": 3682,
    "delete": 1,
    "expect": ["              onChange={(e) => set(\"anexos\", [...(form.anexos || []), ...Array.from(e.target.files || []).map((a) => ({ id: uid(), nome: a.name }))])} />"],
    "insert": ["              onChange={(e) => set(\"anexos\", [...(form.anexos || []), ...Array.from(e.target.files || []).map((a) => ({ id: uid(), nome: a.name, arquivo: a }))])} />"]
  },
  {
    "start": 3958,
    "delete": 0,
    "expect": [],
    "insert": ["  const logoRef = useRef(null);"]
  },
  {
    "start": 3959,
    "delete": 0,
    "expect": [],
    "insert": [
      "  const enviarLogo = async (file) => {",
      "    if (!file) return;",
      "    try { const logo = await uploadLogoEmpresaDB(file, empresaId); const prox = { ...f, logoPath: logo.path, logoUrl: logo.url }; setF(prox); setEmpresa(prox); aviso(\"Logo atualizado\"); }",
      "    catch (e) { aviso(mensagemErro(e)); }",
      "  };"
    ]
  },
  {
    "start": 3969,
    "delete": 1,
    "expect": ["              <div className=\"w-16 h-16 rounded-2xl bg-slate-900 text-white flex items-center justify-center text-xl font-semibold shrink-0\">{iniciais(f.nome)}</div>"],
    "insert": ["              <div className=\"w-16 h-16 rounded-2xl bg-slate-900 text-white flex items-center justify-center text-xl font-semibold shrink-0 overflow-hidden\">{f.logoUrl ? <img src={f.logoUrl} alt=\"Logo da empresa\" className=\"w-full h-full object-cover\" /> : iniciais(f.nome)}</div>"]
  },
  {
    "start": 3971,
    "delete": 1,
    "expect": ["                <Btn variant=\"soft\" size=\"sm\" onClick={() => aviso(\"Envio de logo depende do armazenamento de arquivos, ainda não conectado\")}>Enviar logo</Btn>"],
    "insert": [
      "                <input ref={logoRef} type=\"file\" accept=\"image/jpeg,image/png,image/webp\" className=\"hidden\" onChange={(e) => enviarLogo(e.target.files?.[0])} />",
      "                <Btn variant=\"soft\" size=\"sm\" onClick={() => logoRef.current?.click()}>Enviar logo</Btn>"
    ]
  }
]