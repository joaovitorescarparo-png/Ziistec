import fs from 'node:fs';

function read(path){return fs.readFileSync(path,'utf8');}
function write(path,content){fs.writeFileSync(path,content);}
function replaceOnce(content,needle,replacement,label){
  const first=content.indexOf(needle);
  if(first<0) throw new Error(`RC1C_W1 missing ${label}`);
  if(content.indexOf(needle,first+needle.length)>=0) throw new Error(`RC1C_W1 duplicate ${label}`);
  return content.slice(0,first)+replacement+content.slice(first+needle.length);
}
function replaceBlock(content,startNeedle,endNeedle,replacement,label){
  const start=content.indexOf(startNeedle);
  if(start<0) throw new Error(`RC1C_W1 missing start ${label}`);
  const end=content.indexOf(endNeedle,start+startNeedle.length);
  if(end<0) throw new Error(`RC1C_W1 missing end ${label}`);
  return content.slice(0,start)+replacement+content.slice(end);
}

const machine=`export const SPEECH_STATUS = Object.freeze({\n  IDLE:'idle', LISTENING:'listening', PROCESSING:'processing', SUCCESS:'success', ERROR:'error',\n});\n\nexport const initialSpeechState={status:SPEECH_STATUS.IDLE,error:'',lastSuccessAt:0};\n\nexport function speechReducer(state=initialSpeechState,action={}){\n  switch(action.type){\n    case 'START': return {status:SPEECH_STATUS.LISTENING,error:'',lastSuccessAt:state.lastSuccessAt||0};\n    case 'STOP': return {...state,status:SPEECH_STATUS.PROCESSING,error:''};\n    case 'SUCCESS': return {status:SPEECH_STATUS.SUCCESS,error:'',lastSuccessAt:Date.now()};\n    case 'ERROR': return {...state,status:SPEECH_STATUS.ERROR,error:String(action.message||'Não consegui usar o ditado agora.')};\n    case 'CANCEL':\n    case 'RESET': return {...initialSpeechState,lastSuccessAt:state.lastSuccessAt||0};\n    default: return state;\n  }\n}\n\nexport function appendSpeechText(base,addition){\n  const left=String(base||'').trimEnd();\n  const right=String(addition||'').trim();\n  if(!right) return String(base||'');\n  return left ? \\`${'${left}'} ${'${right}'}\\` : right;\n}\n\nexport function speechErrorMessage(code){\n  const key=String(code||'').toLowerCase();\n  if(key==='not-allowed'||key==='service-not-allowed') return 'Permita o acesso ao microfone para usar o ditado.';\n  if(key==='audio-capture') return 'Não encontrei um microfone disponível. Você pode continuar digitando.';\n  if(key==='network') return 'Sem conexão para o reconhecimento de voz agora. Você pode continuar digitando.';\n  if(key==='no-speech') return 'Não ouvi nenhuma fala. Tente novamente quando estiver pronto.';\n  if(key==='timeout') return 'O ditado demorou demais e foi encerrado. Tente novamente.';\n  if(key==='aborted') return '';\n  return 'Não consegui usar o microfone agora. Você pode continuar digitando.';\n}\n\nexport function isAppleMobile(userAgent=''){\n  const ua=String(userAgent||'');\n  return /iPhone|iPad|iPod/i.test(ua) || (/Macintosh/i.test(ua)&&/Mobile/i.test(ua));\n}\n\nexport function isSpeechBusy(status){\n  return status===SPEECH_STATUS.LISTENING||status===SPEECH_STATUS.PROCESSING;\n}\n`;
write('src/lib/speechStateMachine.js',machine);

const hook=`import { useCallback, useEffect, useReducer, useRef, useState } from 'react';\nimport { SPEECH_STATUS, initialSpeechState, isAppleMobile, isSpeechBusy, speechErrorMessage, speechReducer } from '../lib/speechStateMachine';\n\nconst getSpeechCtor=()=>{\n  if(typeof window==='undefined') return null;\n  return window.SpeechRecognition||window.webkitSpeechRecognition||null;\n};\n\nexport default function useSpeechInput({language='pt-BR',onText,maxDurationMs=30000,processingTimeoutMs=5000}={}){\n  const recognitionRef=useRef(null);\n  const onTextRef=useRef(onText);\n  const activeRef=useRef(false);\n  const cancelledRef=useRef(false);\n  const finalRef=useRef('');\n  const maxTimerRef=useRef(null);\n  const processingTimerRef=useRef(null);\n  const resetTimerRef=useRef(null);\n  const [supported,setSupported]=useState(false);\n  const [state,dispatch]=useReducer(speechReducer,initialSpeechState);\n\n  useEffect(()=>{onTextRef.current=onText;},[onText]);\n  useEffect(()=>{setSupported(Boolean(getSpeechCtor()));},[]);\n\n  const clearTimers=useCallback(()=>{\n    for(const ref of [maxTimerRef,processingTimerRef,resetTimerRef]){if(ref.current){clearTimeout(ref.current);ref.current=null;}}\n  },[]);\n  const emit=useCallback((finalText='',interimText='')=>{\n    const committed=finalRef.current.trim();\n    const interim=String(interimText||'').trim();\n    onTextRef.current?.({finalText:String(finalText||'').trim(),interimText:interim,text:[committed,interim].filter(Boolean).join(' ').trim()});\n  },[]);\n  const scheduleIdle=useCallback(()=>{\n    if(resetTimerRef.current) clearTimeout(resetTimerRef.current);\n    resetTimerRef.current=setTimeout(()=>dispatch({type:'RESET'}),900);\n  },[]);\n\n  const finishError=useCallback((codeOrMessage)=>{\n    clearTimers();activeRef.current=false;recognitionRef.current=null;\n    const message=String(codeOrMessage||'').includes(' ') ? String(codeOrMessage) : speechErrorMessage(codeOrMessage);\n    dispatch({type:'ERROR',message:message||'Ditado interrompido.'});\n  },[clearTimers]);\n\n  const cancel=useCallback(()=>{\n    cancelledRef.current=true;clearTimers();\n    const rec=recognitionRef.current;recognitionRef.current=null;activeRef.current=false;\n    try{rec?.abort();}catch{/* noop */}\n    emit('', '');dispatch({type:'CANCEL'});\n  },[clearTimers,emit]);\n\n  const stop=useCallback(()=>{\n    if(!activeRef.current) return false;\n    dispatch({type:'STOP'});\n    if(maxTimerRef.current){clearTimeout(maxTimerRef.current);maxTimerRef.current=null;}\n    processingTimerRef.current=setTimeout(()=>{\n      const rec=recognitionRef.current;\n      try{rec?.abort();}catch{/* noop */}\n      finishError('timeout');\n    },Math.max(1500,processingTimeoutMs));\n    try{recognitionRef.current?.stop();return true;}catch{finishError('Não consegui finalizar o ditado. Tente novamente.');return false;}\n  },[finishError,processingTimeoutMs]);\n\n  const start=useCallback(()=>{\n    if(activeRef.current||isSpeechBusy(state.status)) return false;\n    clearTimers();cancelledRef.current=false;finalRef.current='';\n    const SpeechRecognition=getSpeechCtor();\n    if(!SpeechRecognition){setSupported(false);dispatch({type:'ERROR',message:'Ditado por voz não é suportado neste navegador. Use a digitação ou o microfone do teclado do celular.'});return false;}\n    const recognition=new SpeechRecognition();\n    recognitionRef.current=recognition;activeRef.current=true;\n    recognition.lang=language;\n    recognition.continuous=!isAppleMobile(typeof navigator==='undefined'?'':navigator.userAgent);\n    recognition.interimResults=true;\n    recognition.maxAlternatives=1;\n    recognition.onstart=()=>dispatch({type:'START'});\n    recognition.onresult=(event)=>{\n      let finalText='';let interimText='';\n      for(let i=event.resultIndex;i<event.results.length;i+=1){\n        const text=event.results[i]?.[0]?.transcript||'';\n        if(event.results[i].isFinal) finalText+=\\`${'${text}'} \\`; else interimText+=text;\n      }\n      const cleanFinal=finalText.trim();\n      if(cleanFinal) finalRef.current=[finalRef.current.trim(),cleanFinal].filter(Boolean).join(' ');\n      emit(cleanFinal,interimText);\n    };\n    recognition.onerror=(event)=>{\n      const code=String(event?.error||'');\n      if(code==='aborted'&&cancelledRef.current) return;\n      finishError(code||'unknown');\n    };\n    recognition.onend=()=>{\n      if(processingTimerRef.current){clearTimeout(processingTimerRef.current);processingTimerRef.current=null;}\n      if(maxTimerRef.current){clearTimeout(maxTimerRef.current);maxTimerRef.current=null;}\n      recognitionRef.current=null;activeRef.current=false;\n      if(cancelledRef.current){cancelledRef.current=false;dispatch({type:'CANCEL'});return;}\n      if(finalRef.current.trim()){dispatch({type:'SUCCESS'});scheduleIdle();}\n      else dispatch({type:'ERROR',message:speechErrorMessage('no-speech')});\n    };\n    dispatch({type:'START'});\n    maxTimerRef.current=setTimeout(()=>{\n      try{recognitionRef.current?.abort();}catch{/* noop */}\n      finishError('timeout');\n    },Math.max(5000,maxDurationMs));\n    try{recognition.start();return true;}catch(error){\n      if(String(error?.name||'')==='InvalidStateError') return false;\n      finishError('Não consegui iniciar o microfone. Tente novamente.');return false;\n    }\n  },[clearTimers,emit,finishError,language,maxDurationMs,scheduleIdle,state.status]);\n\n  useEffect(()=>()=>cancel(),[cancel]);\n\n  return {\n    supported, status:state.status, error:state.error,\n    listening:state.status===SPEECH_STATUS.LISTENING,\n    processing:state.status===SPEECH_STATUS.PROCESSING,\n    success:state.status===SPEECH_STATUS.SUCCESS,\n    busy:isSpeechBusy(state.status),\n    start,stop,cancel,retry:start,\n  };\n}\n`;
write('src/hooks/useSpeechInput.js',hook);

let legacy=read('src/legacy/ZiisTecApp.jsx');
legacy=replaceOnce(legacy,
'import { carregarChecklistOSV2DB, marcarRetornoOSV2DB, novoReturnRequestId } from "../lib/checklistReturnV2Api";\n',
'import { carregarChecklistOSV2DB, marcarRetornoOSV2DB, novoReturnRequestId } from "../lib/checklistReturnV2Api";\nimport useSpeechInput from "../hooks/useSpeechInput";\n',
'legacy speech import');
const legacySpeech=`function usarReconhecimento() {\n  const callbackRef = useRef(null);\n  const speech = useSpeechInput({ onText: ({ text }) => { if (text) callbackRef.current?.(text); } });\n  const iniciar = (aoTexto) => { callbackRef.current = aoTexto; return speech.start(); };\n  return {\n    suportado: speech.supported, ouvindo: speech.listening, processando: speech.processing,\n    sucesso: speech.success, estado: speech.status, erro: speech.error || null,\n    iniciar, parar: speech.stop, cancelar: speech.cancel,\n  };\n}\n\n`;
legacy=replaceBlock(legacy,'function usarReconhecimento() {','/* Componente único de ditado.',legacySpeech+'/* Componente único de ditado.','legacy speech hook');
const campoStart='function CampoVoz({ valor = "", onChange, placeholder, rows = 4, destaque, dica }) {';
const campoEnd='\n\nasync function chamarIA(prompt) {';
const campo=`function CampoVoz({ valor = "", onChange, placeholder, rows = 4, destaque, dica }) {\n  const { suportado, ouvindo, processando, sucesso, erro, iniciar, parar, cancelar } = usarReconhecimento();\n  const baseRef = useRef("");\n  const comecar = () => {\n    baseRef.current = valor.trim() ? valor.trimEnd() + " " : "";\n    iniciar((t) => onChange((baseRef.current + t).trimEnd()));\n  };\n  const ativo = ouvindo || processando;\n  const alternar = () => (ouvindo ? parar() : processando ? cancelar() : comecar());\n\n  return (\n    <div>\n      <div className="relative">\n        <Textarea rows={rows} value={valor} onChange={(e) => onChange(e.target.value)} placeholder={placeholder} aria-label={placeholder}\n          className={cx(suportado && !destaque && "pr-14", ativo && "ring-2 ring-teal-600")} />\n        {suportado && !destaque && (\n          <button type="button" onClick={alternar} aria-label={ouvindo ? "Parar ditado" : processando ? "Cancelar transcrição" : "Ditar por voz"}\n            title={ouvindo ? "Parar ditado" : processando ? "Cancelar transcrição" : "Ditar por voz"}\n            className={cx("absolute right-2.5 bottom-2.5 min-h-11 min-w-11 p-2.5 rounded-lg transition-colors", ring,\n              ativo ? "bg-slate-900 text-white" : "text-slate-400 hover:bg-slate-100 hover:text-slate-700")}>\n            {ativo ? <MicOff className="w-[18px] h-[18px]" /> : <Mic className="w-[18px] h-[18px]" />}\n          </button>\n        )}\n      </div>\n\n      {suportado && destaque && (\n        <div className="mt-3 grid gap-2 min-[390px]:grid-cols-[1fr_auto]">\n          <Btn className="w-full" size="lg" variant={ativo ? "dark" : "primary"} icon={ativo ? MicOff : Mic} onClick={alternar}>\n            {ouvindo ? "Parar ditado" : processando ? "Transcrevendo..." : valor.trim() ? "Continuar falando" : "Falar o que foi feito"}\n          </Btn>\n          {ativo && <Btn variant="ghost" onClick={cancelar}>Cancelar</Btn>}\n        </div>\n      )}\n\n      <div className="flex items-center gap-3 mt-2 flex-wrap min-h-[18px]">\n        {ouvindo ? <span className="inline-flex items-center gap-2 text-[12.5px] font-medium text-teal-800"><span className="w-2 h-2 rounded-full bg-rose-500" aria-hidden="true" />Ouvindo... pode falar.</span>\n          : processando ? <span className="text-[12.5px] font-medium text-sky-700">Transcrevendo...</span>\n          : sucesso ? <span className="text-[12.5px] font-medium text-emerald-700">Texto inserido.</span>\n          : <>{!suportado && <span className="text-[12px] text-slate-400">Ditado indisponível neste navegador. No iPhone/Android, use também o microfone do teclado; a digitação continua disponível.</span>}{suportado && dica && <span className="text-[12px] text-slate-400">{dica}</span>}</>}\n        {ativo && !destaque && <button type="button" onClick={cancelar} className="min-h-11 px-3 rounded-lg text-[12px] font-semibold text-slate-600 hover:bg-slate-100">Cancelar</button>}\n      </div>\n      {erro && <div className="mt-1 flex flex-wrap items-center gap-2"><p className="text-[12.5px] text-rose-700">{erro}</p><button type="button" onClick={comecar} className="min-h-11 px-3 rounded-lg text-[12px] font-semibold text-teal-800 hover:bg-teal-50">Tentar novamente</button></div>}\n    </div>\n  );\n}`;
legacy=replaceBlock(legacy,campoStart,campoEnd,campo+campoEnd,'CampoVoz');
write('src/legacy/ZiisTecApp.jsx',legacy);

let wo=read('src/screens/v2/WorkOrderMemoryBaseV2.jsx');
wo=replaceOnce(wo,
"{interim&&<p className=\"mt-2 text-xs italic text-slate-500\">Ouvindo: {interim}</p>}{speech.error&&<p className=\"mt-2 text-xs text-amber-300\">{speech.error}</p>}",
"{interim&&<p className=\"mt-2 text-xs italic text-slate-500\">Ouvindo: {interim}</p>}{speech.processing&&<p className=\"mt-2 text-xs font-semibold text-sky-300\">Transcrevendo...</p>}{speech.success&&<p className=\"mt-2 text-xs font-semibold text-emerald-300\">Texto inserido.</p>}{speech.error&&<div className=\"mt-2 flex flex-wrap items-center gap-2 text-xs text-amber-300\"><span>{speech.error}</span><button type=\"button\" onClick={speech.retry} className=\"min-h-11 rounded-lg border border-amber-300/30 px-3 font-bold\">Tentar novamente</button></div>}",
'work order speech status');
wo=replaceOnce(wo,
"<button type=\"button\" onClick={speech.listening?speech.stop:speech.start} className={`absolute right-3 top-3 inline-flex min-h-11 min-w-11 items-center justify-center rounded-xl p-2.5 focus:outline-none focus-visible:ring-2 focus-visible:ring-sky-300 ${speech.listening?'bg-rose-500 text-white':'bg-white/10 text-sky-300'}`} title={speech.listening?'Parar ditado':'Ditar relato'}>{speech.listening?<MicOff size={18}/>:<Mic size={18}/>}</button>",
"<button type=\"button\" onClick={speech.listening?speech.stop:speech.processing?speech.cancel:speech.start} className={`absolute right-3 top-3 inline-flex min-h-11 min-w-11 items-center justify-center rounded-xl p-2.5 focus:outline-none focus-visible:ring-2 focus-visible:ring-sky-300 ${speech.busy?'bg-rose-500 text-white':'bg-white/10 text-sky-300'}`} title={speech.listening?'Parar ditado':speech.processing?'Cancelar transcrição':'Ditar relato'}>{speech.busy?<MicOff size={18}/>:<Mic size={18}/>}</button>",
'work order speech control');
write('src/screens/v2/WorkOrderMemoryBaseV2.jsx',wo);

let quote=read('src/screens/v2/QuoteAIBaseV2.jsx');
quote=replaceOnce(quote,
"{interim&&<p className=\"mt-2 rounded-xl bg-slate-100 px-3 py-2 text-xs italic text-slate-500\">Ouvindo: {interim}</p>}{voice.error&&<p className=\"mt-2 text-xs text-amber-700\">{voice.error}</p>}",
"{interim&&<p className=\"mt-2 rounded-xl bg-slate-100 px-3 py-2 text-xs italic text-slate-500\">Ouvindo: {interim}</p>}{voice.processing&&<p className=\"mt-2 text-xs font-semibold text-sky-700\">Transcrevendo...</p>}{voice.success&&<p className=\"mt-2 text-xs font-semibold text-emerald-700\">Texto inserido.</p>}{voice.error&&<div className=\"mt-2 flex flex-wrap items-center gap-2 text-xs text-amber-700\"><span>{voice.error}</span><button type=\"button\" onClick={voice.retry} className=\"min-h-11 rounded-lg border border-amber-200 px-3 font-bold\">Tentar novamente</button></div>}",
'quote voice status');
quote=replaceOnce(quote,
"<Button variant={voice.listening?'secondary':'soft'} onClick={voice.listening?voice.stop:voice.start} disabled={!voice.supported}>{voice.listening?<><MicOff size={16}/>Parar microfone</>:<><Mic size={16}/>Ditado por voz</>}</Button>",
"<Button variant={voice.busy?'secondary':'soft'} onClick={voice.listening?voice.stop:voice.processing?voice.cancel:voice.start} disabled={!voice.supported}>{voice.listening?<><MicOff size={16}/>Parar ditado</>:voice.processing?<><Loader2 className=\"animate-spin\" size={16}/>Transcrevendo...</>:<><Mic size={16}/>Ditado por voz</>}</Button>{voice.busy&&<Button variant=\"secondary\" onClick={voice.cancel}>Cancelar</Button>}",
'quote voice controls');
quote=replaceOnce(quote,
"<Button variant={customerVoice.listening?'secondary':'soft'} onClick={customerVoice.listening?customerVoice.stop:customerVoice.start} disabled={!customerVoice.supported}>{customerVoice.listening?<><MicOff size={16}/>Parar</>:<><Mic size={16}/>Ditar mensagem</>}</Button>",
"<div className=\"flex flex-wrap gap-2\"><Button variant={customerVoice.busy?'secondary':'soft'} onClick={customerVoice.listening?customerVoice.stop:customerVoice.processing?customerVoice.cancel:customerVoice.start} disabled={!customerVoice.supported}>{customerVoice.listening?<><MicOff size={16}/>Parar</>:customerVoice.processing?<><Loader2 className=\"animate-spin\" size={16}/>Transcrevendo...</>:<><Mic size={16}/>Ditar mensagem</>}</Button>{customerVoice.busy&&<Button variant=\"secondary\" onClick={customerVoice.cancel}>Cancelar</Button>}</div>",
'customer voice controls');
quote=replaceOnce(quote,
"{customerInterim&&<p className=\"mt-2 text-xs italic text-slate-500\">Ouvindo: {customerInterim}</p>}{customerVoice.error&&<p className=\"mt-2 text-xs text-amber-700\">{customerVoice.error}</p>}",
"{customerInterim&&<p className=\"mt-2 text-xs italic text-slate-500\">Ouvindo: {customerInterim}</p>}{customerVoice.processing&&<p className=\"mt-2 text-xs font-semibold text-sky-700\">Transcrevendo...</p>}{customerVoice.success&&<p className=\"mt-2 text-xs font-semibold text-emerald-700\">Texto inserido.</p>}{customerVoice.error&&<p className=\"mt-2 text-xs text-amber-700\">{customerVoice.error}</p>}",
'customer voice status');
write('src/screens/v2/QuoteAIBaseV2.jsx',quote);

const test=`import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport { SPEECH_STATUS, appendSpeechText, initialSpeechState, isAppleMobile, isSpeechBusy, speechErrorMessage, speechReducer } from '../../src/lib/speechStateMachine.js';\n\ntest('voice state machine follows idle -> listening -> processing -> success -> idle',()=>{\n  let state=initialSpeechState;\n  state=speechReducer(state,{type:'START'}); assert.equal(state.status,SPEECH_STATUS.LISTENING);\n  assert.equal(isSpeechBusy(state.status),true);\n  state=speechReducer(state,{type:'STOP'}); assert.equal(state.status,SPEECH_STATUS.PROCESSING);\n  state=speechReducer(state,{type:'SUCCESS'}); assert.equal(state.status,SPEECH_STATUS.SUCCESS);\n  state=speechReducer(state,{type:'RESET'}); assert.equal(state.status,SPEECH_STATUS.IDLE);\n});\n\ntest('voice error is recoverable by a new START and cancel returns idle',()=>{\n  let state=speechReducer(initialSpeechState,{type:'ERROR',message:'falhou'});\n  assert.equal(state.status,SPEECH_STATUS.ERROR);assert.equal(state.error,'falhou');\n  state=speechReducer(state,{type:'START'});assert.equal(state.status,SPEECH_STATUS.LISTENING);assert.equal(state.error,'');\n  state=speechReducer(state,{type:'CANCEL'});assert.equal(state.status,SPEECH_STATUS.IDLE);\n});\n\ntest('dictation appends without erasing existing manual text',()=>{\n  assert.equal(appendSpeechText('Texto existente','novo trecho'),'Texto existente novo trecho');\n  assert.equal(appendSpeechText('','novo trecho'),'novo trecho');\n  assert.equal(appendSpeechText('Texto existente   ','  novo trecho  '),'Texto existente novo trecho');\n});\n\ntest('speech errors provide actionable fallback and Apple mobile is detected',()=>{\n  assert.match(speechErrorMessage('not-allowed'),/microfone/i);\n  assert.match(speechErrorMessage('timeout'),/encerrado/i);\n  assert.equal(isAppleMobile('Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X)'),true);\n  assert.equal(isAppleMobile('Mozilla/5.0 (Linux; Android 16)'),false);\n});\n`;
write('tests/blockers/rc1c_voice_state_machine.test.mjs',test);

let pkg=JSON.parse(read('package.json'));
const marker=' tests/blockers/rc1b_upload_security_contract.test.mjs';
if(!pkg.scripts['verify:v2'].includes('rc1c_voice_state_machine')){
  pkg.scripts['verify:v2']=pkg.scripts['verify:v2'].replace(marker,marker+' tests/blockers/rc1c_voice_state_machine.test.mjs');
}
write('package.json',JSON.stringify(pkg,null,2)+'\n');
console.log('RC1C_WAVE1_APPLY=PASS');
